# 🚀 GitHub Pages & VS Code 開発完全マニュアル

このドキュメントは、GitHub と VS Code を連携させて、自分専用の Web サイトを構築・運用するための詳細な手順書です。
今回お渡ししたサイト一式（`index.html` や `assets/` フォルダなど）を、GitHub で公開し、VS Code で編集・保存していく流れを最初から最後まで説明します。

---

## 0. 全体像（最初に読んでください）

作業の流れはとてもシンプルです。

```
① ツールを入れる（VS Code と Git）
② GitHub に「公開用の置き場所（リポジトリ）」を作る
③ その置き場所を VS Code で自分のPCに取り込む（クローン）
④ 今回のサイトのファイルをそのフォルダにコピーする
⑤ VS Code で編集 → 保存（Ctrl + S）
⑥ 変更を GitHub に送る（コミット → プッシュ）
⑦ 数十秒待つと、世界中に公開されているサイトが自動で更新される
```

一度 ①〜④ を済ませれば、普段は **⑤→⑥→⑦ のくり返し** だけです。

---

## 1. 準備（ツールのインストール）

開発を始める前に、以下の2つのツールを PC にインストールします。

### ① Visual Studio Code (VS Code)
- **役割**: コードを書くための高機能エディタ。
- [公式サイト](https://code.visualstudio.com/) からダウンロード。

### ② Git (ギット)
- **役割**: PC と GitHub の間でファイルを転送・同期するための心臓部。
- [公式サイト](https://git-scm.com/) からダウンロード。
- ※ Windows の場合：インストール時の設定はすべて「Next」で OK です。
- ※ インストール後、いちど PC を再起動するか、VS Code を再起動すると Git が認識されます。

---

## 2. GitHub リポジトリの作成（公開用）

実際に世界に公開する Web サイト用の場所を作ります。

1. GitHub 右上の「+」→「New repository」。
2. **Repository name**: `[自分のユーザー名].github.io`
    - **注意**: 1文字でも間違うと公開されません（例：ユーザー名が `harukiemori` なら `harukiemori.github.io`）。
    - すべて小文字で入力してください。
3. **Public** を選択（無料プランで公開するには Public 必須）。
4. 「Add a README file」にチェックを入れて作成。

---

## 3. VS Code との連携（クローン）

GitHub 上の空のリポジトリを、自分の PC に「フォルダ」として取り込みます。

1. **VS Code を起動**。
2. 左側の「エクスプローラー」アイコン →「**リポジトリをクローン (Clone Repository)**」をクリック。
3. 「**GitHub から複製 (Clone from GitHub)**」を選択。
4. ブラウザが開くので、GitHub へのアクセスを許可（Authorize）する。
5. リポジトリ一覧から `[ユーザー名].github.io` を選ぶ。
6. PC 上の保存場所（デスクトップなど）を選んで「Select as Repository Location」。
7. 右下のポップアップで「**開く (Open)**」をクリック。

これで、PC の中に GitHub と同期するフォルダができました。

---

## 4. 拡張機能の設定（効率化）

VS Code の左側「四角いアイコン（拡張機能）」から以下を検索・インストールします。

| 拡張機能名 | 役割 |
| :--- | :--- |
| **Japanese Language Pack** | VS Code を日本語メニューにします。 |
| **Live Server** | 保存するたびにブラウザが自動更新され、プレビューできます。 |

---

## 5. 今回のサイト一式をフォルダに入れる（最重要）

ここが「お渡ししたファイルを実際に使う」ステップです。

1. ダウンロードしたサイト一式（フォルダ）の中身を確認します。次のファイル・フォルダが入っています。

    ```
    index.html
    cv.html
    works.html
    presentations.html
    activities.html
    resources.html
    contact.html
    README.md
    開発マニュアル.md
    assets/   ← この中に css / js / img フォルダが入っています
    ```

2. これらを **すべて**、手順3でクローンしたフォルダ（`[ユーザー名].github.io`）の中に **コピー＆ペースト** します。
    - `assets` フォルダは中身ごとそのまま入れてください（フォルダ構成を崩さないことが大切です）。
    - GitHub で最初に作られた `README.md` は、今回の `README.md` で上書きして問題ありません。

3. VS Code に戻ると、左側のファイル一覧にコピーしたファイルが現れます。

4. **プレビューしてみる**：`index.html` を右クリック →「**Open with Live Server**」を選ぶと、ブラウザでサイトが表示されます。
    - 右上の月／太陽アイコンでダーク／ライトの切り替えができます。
    - ホーム右側の球体（ブロッホ球）はマウスでドラッグして回せます。

> 補足：`index.html` 内の `Last updated · May 27, 2026` の日付を、更新するたびに書き換えると親切です。

---

## 6. 編集 → 保存 → 公開 のサイクル（毎日の作業）

ここからが普段の運用です。例として「新しい論文を1本追加する」流れを示します。

### 6-1. 編集する
- 論文・発表のデータは **`assets/js/data.js`** にまとまっています。
- 例：プレプリントを足すときは、`window.PREPRINTS` の配列に次のように追記します。

    ```js
    window.PREPRINTS.push({
      year: 2027,
      authors: [{ name: "H. Emori", me: true }, { name: "Co Author" }],
      title: "新しい論文のタイトル",
      venue: "Preprint",
      links: [{ label: "arXiv:2701.00000", href: "https://arxiv.org/abs/2701.00000" }]
    });
    ```
    - `me: true` を付けた著者名は、自分の名前として強調表示されます。
    - 並び順は年で自動ソートされるので、場所は気にしなくて大丈夫です。
- 文章（Home や CV など）を直したいときは、対応する `.html` ファイルを開いて編集します。

### 6-2. 保存する
- 編集したら **`Ctrl + S`（Mac は `Cmd + S`）** で保存します。
- Live Server を使っていれば、ブラウザが自動で更新され、見た目をすぐ確認できます。

### 6-3. GitHub に送る（コミット → プッシュ）
保存しただけでは、まだ自分の PC の中だけの変更です。世界に公開するには GitHub へ送ります。

1. VS Code 左側の「**ソース管理 (Source Control)**」アイコン（枝分かれのマーク）をクリック。
2. 変更したファイルの一覧が出ます。上の入力欄に、変更内容のメモ（例：`Add new preprint 2027`）を入力。
3. 「**✓ コミット (Commit)**」ボタンを押す。
    - 「ステージしますか？」と聞かれたら「Yes」または「常に」を選んでください。
4. 続いて「**変更の同期 (Sync Changes)**」または「**プッシュ (Push)**」ボタンを押す。

> コマンドで行いたい場合は、VS Code のターミナル（メニュー →「ターミナル」→「新しいターミナル」）で：
> ```bash
> git add .
> git commit -m "Add new preprint 2027"
> git push
> ```

### 6-4. 公開を確認する
- プッシュ後、**数十秒〜1分ほど** 待ってから `https://[ユーザー名].github.io/` を開く（または再読み込み）。
- 変更が反映されていれば成功です。
- 反映されないときは、ブラウザの **スーパーリロード**（`Ctrl + Shift + R` / Mac は `Cmd + Shift + R`）でキャッシュを更新してみてください。

---

## 7. 最初の1回だけ：GitHub Pages を「オン」にする

クローンしたフォルダに初めてファイルをプッシュしたあと、公開設定を確認します。

1. GitHub のリポジトリページ → **Settings（設定）** → 左メニューの **Pages**。
2. **Source** を「**Deploy from a branch**」に。
3. Branch を「**main**」、フォルダを「**/ (root)**」にして **Save**。
4. 少し待つと、上部に公開 URL（`https://[ユーザー名].github.io/`）が表示されます。

`[ユーザー名].github.io` という名前のリポジトリなら、基本的にこの設定で自動公開されます。

---

## 8. よくあるトラブルと対処

| 症状 | 原因・対処 |
| :--- | :--- |
| サイトが真っ白／404 | リポジトリ名が `[ユーザー名].github.io` になっているか確認。`index.html` がフォルダの **直下** にあるか確認。 |
| デザインが崩れる | `assets` フォルダごとプッシュできているか確認（`assets/css/style.css` が GitHub 上に存在するか）。 |
| 画像が出ない | `assets/img/HE_RIKEN.jpg` を置いたか確認。無い場合は「HE」のロゴが自動表示されます（仕様）。 |
| 変更が反映されない | プッシュ後1分待つ＋スーパーリロード。ソース管理で本当にプッシュ済みか確認。 |
| `git push` で認証を求められる | 画面の指示に従い GitHub にサインイン（ブラウザ認証）すれば OK。 |

---

## 9. 編集のコツ（このサイト特有のポイント）

- **ナビゲーション（上部メニュー）を変える** ときは、7つの HTML ファイルすべての同じ部分を直す必要があります。VS Code の「すべてのファイルから検索・置換」（虫めがねアイコン）を使うと一括で直せます。
- **配色や見た目** は `assets/css/style.css` の先頭にある「テーマトークン」（`--bg`、`--cyan` など）を変えるだけで全体に反映されます。
- **量子ビジュアル**（ブロッホ球・干渉縞・波束）は `assets/js/site.js` にまとまっています。動きを止めたい場合は、各ページ末尾の `startBloch(...)` などの呼び出しを消せば消えます。

---

これで、GitHub で管理しつつ VS Code で快適に編集・公開できる環境が整いました。お疲れさまでした ⚛
